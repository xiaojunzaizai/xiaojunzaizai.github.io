document.addEventListener('DOMContentLoaded', function () {
    var heading = document.querySelector('h1');
    if (heading) {
        heading.textContent = 'Hello, TypeScript and GitHub Pages!';
    }
});
